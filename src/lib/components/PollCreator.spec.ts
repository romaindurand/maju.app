import { describe } from 'vitest';

// Test déplacé vers src/lib/components/PollCreator.svelte.spec.ts (mode navigateur)
// Ce fichier est conservé vide/skip pour éviter l’exécution côté serveur.
describe.skip('PollCreator.svelte (server placeholder)', () => {});
