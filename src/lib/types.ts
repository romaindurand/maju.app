/**
 * Visibility mode for poll participants
 * - 'always': Participants are always visible
 * - 'after_expiration': Participants are visible only after poll expiration
 * - 'never': Participants are never visible
 */
export type ParticipantsVisibility = 'always' | 'after_expiration' | 'never';
